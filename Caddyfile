{$WEB_DOMAIN}:80 {
	redir https://{host}{uri} permanent
}

{$WEB_DOMAIN} {
	reverse_proxy flask_gunicorn_web:5000
}

logs.privatedns.org:443 {
	reverse_proxy portainer:9000
}

# Also add HTTP redirect for logs subdomain
logs.privatedns.org:80 {
	redir https://{host}{uri} permanent
}
